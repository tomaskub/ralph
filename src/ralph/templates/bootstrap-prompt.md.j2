Read `{{ agent_files_directory }}/task.md` and `{{ agent_files_directory }}/context.md`, inspect the code, implement only the ticket scope, do not commit, and update the `{{ agent_files_directory }}/` status and MR files.
